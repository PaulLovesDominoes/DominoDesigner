# Loaded automatically by Vite for `npm run build`. See structure-designer/enabled.ts.
#
# The Structure Designer is unfinished, so a published build hides it. To make a
# build with it visible, set this to true here, or leave this file alone and put
# the same line in a .env.local (which is not committed).
VITE_ENABLE_STRUCTURE_DESIGNER=false